import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

function Footer({ props }) {
    return (
        <View>
            <Text>@NewsLaundry</Text>
        </View>
    )
}
export default Footer;